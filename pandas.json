[
  {
    "question": "What is pandas in Python?",
    "answer": "Pandas is an open-source Python library with powerful and built-in methods to efficiently clean, analyze, and manipulate datasets. Pandas is built on top of the NumPy library i.e., its data structures Series and DataFrame are the upgraded versions of NumPy arrays.",
    "language": "python",
    "code": "import pandas as pd\ndf = pd.DataFrame({\"A\":[1,2,3], \"B\":[4,5,6]})\nprint(df[df[\"A\"] > 1])"
  },
  {
    "question": "What is the difference between a Series and a DataFrame?",
    "answer": " The pandas DataFrame will be in tabular format with multiple rows and columns where each column can be of different data types.<br><br>The Series is a one-dimensional labeled array that can store any data type, but all of its values should be of the same data type. The Series data structure is more like single column of a DataFrame.",
    "language": "python",
    "code": "df = df.dropna()"
  }
]
